function drawBarChart(data, options, element) {
  // data:
  //   [ {x, height, colour}, ... ]
  // options:
  //   {defaultColour, barWidth, }

  /*let barWidth = options.barWidth || 10;

  for (let d of data) {
    let x = d.x;
    let y = 0;
  }*/
}